{
  "name": "enhanced-travel-screening",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "scripts": {
    "db:start": "docker run -p 8000:8000 amazon/dynamodb-local",
    "db:seed": "node database.js",
    "watch": "NODE_ENV=development nodemon main.js",
    "start": "NODE_ENV=production node main.js",
    "pretest": "docker run --rm --name test -p 8000:8000 -d amazon/dynamodb-local && NODE_ENV=test npm run db:seed",
    "test": "NODE_ENV=test jest && docker stop test || docker stop test "
  },
  "dependencies": {
    "aws-sdk": "2.654.0",
    "axios": "0.19.2",
    "body-parser": "1.19.0",
    "crypto": "1.0.1",
    "express": "4.17.1",
    "jsonwebtoken": "8.5.1",
    "node-cache": "5.1.0",
    "passport": "0.4.1",
    "passport-jwt": "4.0.0",
    "passport-local": "1.0.0",
    "puppeteer": "2.1.1",
    "yup": "0.28.3"
  },
  "devDependencies": {
    "eslint": "6.8.0",
    "eslint-config-airbnb-base": "14.1.0",
    "eslint-plugin-import": "2.20.2",
    "jest": "25.3.0",
    "nodemon": "2.0.2",
    "supertest": "4.0.2"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
